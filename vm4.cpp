// Аналитический метод

#include <iostream>
#include <math.h>

using namespace std;

int main() {
// Задаем точность и коэффициент для метода Ньютона
const double eps = 0.001;
const double beta = 0.01;

// Задаем начальные границы интервала
double a = 2.5;
double b = 2.9;
double x;

// Используем метод половинного деления для поиска корня
do {
    x = (a + b) / 2; // Вычисляем середину отрезка
    double f = 3 * cos(2 * x) - x + 0.25; // Вычисляем значение функции в точке x
    if (f > 0) { // Если значение функции больше 0, значит корень находится в левой половине отрезка
        b = x;
    } else { // Иначе корень находится в правой половине отрезка
        a = x;
    }
} while (fabs(b - a) > eps); // Повторяем до тех пор, пока длина отрезка больше заданной точности

// Выводим найденный корень
cout << "Найденный корень: " << x << endl;

return 0;

}